spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.200.156:8848

server:
  port: 9002

management:
  endpoints:
    web:
      exposure:
        include: "*"
        #PREFER_HOST_MODE